# スケジューリング

ある特定の日時だけにACLを適用する、ということも可能です。

本例では、「土曜日の22:00～23:00を外部からのメンテナンス時間とする」という想定で、その日時だけweb-vsと wild-XXX-vs宛のSSHを許可する、という設定を行っています。

実際には、本日の曜日と、今現在時刻の範囲で設定してみてください。

```{toctree}
:maxdepth: 1
:glob:
module01/**
```
